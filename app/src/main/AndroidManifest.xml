<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.san4o.checkers">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_mini"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_mini"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RulesActivity" />
        <activity android:name=".activity.AboutAvtivity" />
        <activity android:name=".activity.GameActivity" />
    </application>

</manifest>